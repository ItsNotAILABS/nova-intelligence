const http=require('http'); const {loadConfig}=require('./paths'); const {stagePermission}=require('./permissions');
const cfg=loadConfig();
function page(){return `<!doctype html><html><head><meta charset="utf-8"><title>NOVA Agent Council</title><style>body{font-family:Arial;margin:30px;max-width:1100px}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.card{border:1px solid #ddd;border-radius:12px;padding:16px}button,input,textarea{width:100%;padding:10px;margin:6px 0}</style></head><body><h1>NOVA Agent Council</h1><p>Local browser workstation. MCP Spine. Repo Intelligence. Capsule Builder. D:\\ vault.</p><div class="grid"><div class="card"><b>Status</b><pre>${JSON.stringify({version:'0.8.0',vault:cfg.vaultRoot,workspace:cfg.workspaceRoot},null,2)}</pre></div><div class="card"><b>Operator Command</b><form method="POST" action="/permission"><input name="app" value="local"><textarea name="action" rows="5">stage command</textarea><button>Stage Permission</button></form></div></div></body></html>`}
http.createServer((req,res)=>{if(req.method==='POST'){let b=''; req.on('data',d=>b+=d); req.on('end',()=>{const params=new URLSearchParams(b); const out=stagePermission(params.get('app')||'local',params.get('action')||'stage command',{}); res.end(`<pre>${JSON.stringify(out,null,2)}</pre><a href="/">Back</a>`)}); return} res.setHeader('content-type','text/html'); res.end(page());}).listen(cfg.browser.port,cfg.browser.host,()=>console.log(`NOVA browser workstation http://${cfg.browser.host}:${cfg.browser.port}`));
