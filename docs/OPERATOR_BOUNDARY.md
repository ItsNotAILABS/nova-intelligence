# Operator Boundary
No silent app use. No silent deletion. No silent deployment. No provider branding leak on public capsules. Exact duplicates require SHA-256 proof. Sandbox commands are deny-by-default unless allowlisted. GitHub, ICP, HERMES, browser, Gmail, Cloudflare, and filesystem changes must stage permission packets before execution.
