# NOVA Recursive Miniverse and Folder Compression

This artifact incorporates the provided Grok/NOVA material as source doctrine for the local runtime.

## Core Claim

Complex systems can be compressed into folders when the folder carries:

- code
- protocol manifests
- mini brain
- mini heart
- recursive shell map
- proof manifest
- operator entrypoint

The folder is not just storage. It becomes a compressed ecosystem packet.

## Three Shells

NOVA uses three recursive shells:

- `MICRO`: fast local command/file/code/probe loops.
- `MESO`: routing, governance, services, protocols, test, and build coordination.
- `MACRO`: business/product/world interface, launch, docs, and external systems.

Inner layers run faster. Outer layers coordinate consequences, product state, and launch action.

## Terminal Builder AI

`ARTIFEX` is the NOVABUILD terminal builder intelligence.

It has:

- mini brain: planning, decomposition, generation, safe execution bias
- mini heart: coherence, care signals, anti-fragmentation, promotion rule

Runtime module:

```text
codex_runtime/terminal_builder_ai.py
```

API:

```text
GET  /terminal-builder/brain
POST /terminal-builder/plan
POST /terminal-builder/create
```

CLI:

```bash
python codex_cli.py builder-plan "recursive miniverse OS with Universal MCP"
python codex_cli.py builder-create "recursive miniverse OS with Universal MCP"
```

NOVABUILD terminal:

```text
build recursive miniverse OS with Universal MCP Career Hub and launcher app
```

## Production Boundary

The terminal builder does not execute arbitrary shell commands. It generates controlled folders, code files, manifests, scripts, zips, and proof.

The generated system promotes only when:

- mini brain exists
- mini heart exists
- recursive shells are declared
- Universal MCP contract exists
- proof manifest exists
- operator README exists
- zip package exists

## MESIE v1.2.0 Intake

The provided material names:

- persistent virtual servers
- Universal MCP
- Career Hub / Career MCP
- ML recursive loop
- watchdog supervision
- folder compression
- V1 release path

This runtime incorporates those as architecture patterns rather than verified external deployment claims.
